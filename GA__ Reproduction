# Needs to be coded
# Takes two random string halves (one left string, the father and one right string, the mother) and checks their fitness
# If fitness > threshold, adds them to firm pool

def reproduction():
  repopulated_firms = []
  new_firm = []
  for i in firm_halves:
    select = random.choices(GA.firm_halves)
    select2 = random.choices(GA.firm_halves)
    # where firm_halves need to be coded as the first and second halves of individual firms in GA.firm
    new_firm.append(select)
    new_firm.append(select2)
 
    # test for fitness
    if GA.fitness 
    
    
    
